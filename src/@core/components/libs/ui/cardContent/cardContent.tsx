import { CardContent } from '@mui/material';
import React from 'react';

interface CardHeaderProps {
    // props...
}

const MUICardContent = (props: CardHeaderProps) => {
    return <CardContent></CardContent>
};

export default MUICardContent;
